# Phase 4 - Step 03: markdown-body CSS variable 전환

## 목표
- `globals.css`의 `.markdown-body` 내 하드코딩된 hex 색상을 CSS variable로 전환

## 작업 항목
- [ ] `.markdown-body` 내 하드코딩 hex 색상 식별
- [ ] shadcn/ui CSS variable로 매핑 및 교체
- [ ] 다크모드 `.dark .markdown-body` 스타일 정리
- [ ] 코드 블록, 테이블, 인용문 등 모든 마크다운 요소 확인

## 완료 조건
- `.markdown-body`에 하드코딩된 hex (#xxx) 색상이 없을 것
- 라이트/다크 모드 마크다운 렌더링 정상

## 참고
- `app/globals.css` 내 `.markdown-body` 섹션
- 0219 옵시디안 문서의 "남은 작업" 항목
