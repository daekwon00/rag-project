# Phase 2 - Step 04: 청킹 전략 개선

## 목표
- 헤딩 기반 분할 + 코드 블록 보존으로 RAG 품질 향상

## 작업 항목
- [x] `lib/chunker.ts` 수정:
  - 마크다운 헤딩(`#`, `##`, `###`) 기준 섹션 분할 우선
  - 코드 블록(```) 내부 분할 방지
  - 섹션이 chunkSize 초과 시 문장 경계로 폴백
  - 기존 `chunkText` 함수 시그니처 유지 (하위호환)
- [x] 테스트 확장: 23개 테스트 (헤딩 분할, 코드 블록 보존)

## 완료 조건
- 헤딩 기준 분할 + 코드 블록 보존 동작
- 기존 테스트 포함 전체 통과

## 참고
- 커밋: `154e05b`
- Agent Teams 4차 (chunker-engineer)
